name: Disabled temporary waiting-order patch workflow v2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary patch workflow disabled. Safe to delete manually."
